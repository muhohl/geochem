% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_number_imputer.R
\name{random_number_imputer}
\alias{random_number_imputer}
\title{Impute below-detection-limit values with random numbers}
\usage{
random_number_imputer(data, columns, split_symbol = "<", digits = 6)
}
\arguments{
\item{data}{A data frame containing at least the columns referenced by
\code{columns}.}

\item{columns}{Integer vector of column positions to process.}

\item{split_symbol}{Character scalar used to identify and split
below-detection values (default \code{"<"}).}

\item{digits}{Number of decimal places to round the imputed random value to
(default \code{6}).}
}
\value{
The input data frame with the selected columns replaced by numeric
vectors, below-detection entries substituted by random values.
}
\description{
Replaces values flagged as below the detection limit (e.g. \code{"<0.05"}) with
a random number drawn uniformly between 0 and the detection limit. This is a
simple single-imputation approach commonly used in geochemistry to handle
censored data before log-transformation or multivariate analysis.
}
\details{
Columns are selected by integer position via \code{columns}. Each selected column
is scanned for strings containing \code{split_symbol}; matching values are split
on that symbol, the detection limit is extracted from the right-hand part,
and a uniform random value in \eqn{[0, \text{limit}]} is substituted. Values
that do not contain \code{split_symbol} are left unchanged. All selected columns
are coerced to numeric on return.
}
\examples{
d <- data.frame(Au = c("0.12", "<0.05", "0.30"), Cu = c("<1", "2.1", "0.8"))
random_number_imputer(d, columns = c(1, 2))

}

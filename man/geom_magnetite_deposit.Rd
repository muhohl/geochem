% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mgt_class_D_B.R
\name{geom_magnetite_deposit}
\alias{geom_magnetite_deposit}
\title{Magnetite discrimination diagram after Dupuis & Beaudoin (2011)}
\usage{
geom_magnetite_deposit(
  labels = TRUE,
  fields = TRUE,
  color_fill = TRUE,
  color = "black",
  textsize = 3,
  scales = TRUE
)
}
\arguments{
\item{labels}{Logical; if \code{TRUE} (default) field name annotations are drawn.}

\item{fields}{Logical; if \code{TRUE} (default) field boundary lines are drawn.}

\item{color_fill}{Logical; if \code{TRUE} (default) fields are filled with
translucent colours.}

\item{color}{Colour used for field boundary lines and label text
(default \code{"black"}).}

\item{textsize}{Font size for the field name annotations (default \code{3}).}

\item{scales}{Logical; if \code{TRUE} (default) log~10~ axis scales and axis
labels are added. Set to \code{FALSE} when layering multiple diagram calls to
avoid duplicate-scale warnings.}
}
\value{
A list of ggplot2 layer objects.
}
\description{
Returns a list of ggplot2 layers that draw the Ti+V vs Al+Mn magnetite
discrimination fields. Add the result directly to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} call
alongside \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}. The x-axis maps to \strong{Ti + V (wt \%)} and
the y-axis to \strong{Al + Mn (wt \%)}, both on log~10~ scales.
}
\details{
Six deposit-type fields are drawn: Skarn, Fe-Ti & V, Porphyry, Kiruna,
IOCG, and BIF.
}
\examples{
# Overlay on scatter plot of magnetite data
# (columns Ti_V = Ti + V wt\%, Al_Mn = Al + Mn wt\%)
\dontrun{
ggplot2::ggplot(magnetite_data, ggplot2::aes(x = Ti_V, y = Al_Mn)) +
    geom_magnetite_deposit() +
    ggplot2::geom_point()
}

# Template only (no data)
ggplot2::ggplot() + geom_magnetite_deposit()
}
\references{
Dupuis, C., & Beaudoin, G. (2011). Discriminant diagrams for
iron oxide trace element fingerprinting of mineral deposit types.
\emph{Mineralium Deposita}, 46(4), 319–335.
\doi{10.1007/s00126-011-0334-y}
}

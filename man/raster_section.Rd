% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster_section.R
\name{raster_section}
\alias{raster_section}
\title{Raster cross-section for drillhole data}
\usage{
raster_section(
  data,
  x = "x",
  y = "y",
  z = "z",
  view_angle = 0,
  n = 100,
  agg_fn = mean,
  group_col = NULL
)
}
\arguments{
\item{data}{Data frame with drillhole coordinate and assay columns.}

\item{x}{Name of the easting column (default \code{"x"}).}

\item{y}{Name of the northing column (default \code{"y"}).}

\item{z}{Name of the depth column (default \code{"z"}).}

\item{view_angle}{Viewing azimuth in degrees (default \code{0}). See
\code{\link[=drillhole_section]{drillhole_section()}} for the cardinal-direction mapping.}

\item{n}{Number of grid cells along each axis (default \code{100}), producing an
n×n grid in projected-distance × depth space.}

\item{agg_fn}{Aggregation function applied to all numeric columns within
each grid cell. Default \code{mean}. Pass \code{median} for median aggregation.}

\item{group_col}{Name of a categorical column (e.g. \code{"lithology"}) to use
as an additional grouping variable. When supplied the output contains one
row per grid cell × group level. \code{NULL} (default) aggregates across all
observations in each cell.}
}
\value{
A data frame with columns \code{proj_x_raster} (binned projected
horizontal distance), \code{z_raster} (binned depth), \code{index_x}, \code{index_z},
all original columns aggregated by \code{agg_fn}, and \code{n} (observation count
per cell).
}
\description{
Projects drillhole x/y coordinates onto a vertical section plane (using the
same rotation as \code{\link[=drillhole_section]{drillhole_section()}}) and bins the resulting 2D positions
(projected horizontal distance × depth) into a regular n×n grid. Because
dense drillhole datasets often have many overlapping samples when viewed in
cross-section, each grid cell is collapsed to a single representative value
via \code{agg_fn}.
}
\details{
The horizontal projection follows:
\deqn{x_{\text{proj}} = x \cos(\theta) - y \sin(\theta)}
where \eqn{\theta} is \code{view_angle} in degrees.
}
\examples{
dh <- data.frame(
  hole_id   = rep(paste0("DH-0", 1:3), each = 20),
  x         = rep(c(100, 200, 300), each = 20),
  y         = rep(c(100, 150, 120), each = 20),
  z         = rep(seq(0, 190, by = 10), 3),
  Au_ppm    = runif(60),
  lithology = sample(c("Granite", "Schist"), 60, replace = TRUE)
)

raster_section(dh, view_angle = 45, n = 20, group_col = "lithology")
}

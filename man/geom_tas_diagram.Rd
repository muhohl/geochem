% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tas_diagram.R
\name{geom_tas_diagram}
\alias{geom_tas_diagram}
\title{Total Alkali Silica diagram}
\usage{
geom_tas_diagram(fields = TRUE, labels = TRUE)
}
\arguments{
\item{fields}{If \code{TRUE} (default), draw the field boundary lines.}

\item{labels}{If \code{TRUE} (default), draw field name labels.}
}
\value{
A list of ggplot2 layer objects.
}
\description{
Template for the Total Alkali Silica (TAS) diagram after Le Maitre (1989).
The function returns a list of ggplot2 layers that can be added directly to
a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} call. Map \code{SiO2} (wt \%) to x and \code{Na2O + K2O} (wt \%)
to y; colour or shape aesthetics should be placed inside \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}
rather than the global \code{\link[ggplot2:aes]{ggplot2::aes()}} so they are not inherited by the
diagram lines.
}
\details{
Field labels are drawn with \code{\link[ggfittext:geom_fit_text]{ggfittext::geom_fit_text()}}: the font size is
automatically reduced so the text fits within each field's bounding box.
}
\examples{
ggplot2::ggplot() +
    geochem::geom_tas_diagram()
}

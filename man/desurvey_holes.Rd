% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desurvey_holes.R
\name{desurvey_holes}
\alias{desurvey_holes}
\title{Desurvey drillhole traces}
\usage{
desurvey_holes(
  collar,
  survey,
  holeid = "HOLEID",
  east = "BestEast_D",
  north = "BestNorth_D",
  elev = "BestElev_D",
  depth = "Depth_M",
  azimuth = "AZIMUTH",
  dip = "DIP"
)
}
\arguments{
\item{collar}{Data frame with one row per drillhole containing collar
coordinates.}

\item{survey}{Data frame with survey measurements at regular depth intervals.}

\item{holeid}{Name of the hole identifier column present in both data frames
(default \code{"HOLEID"}).}

\item{east}{Name of the easting column in \code{collar} (default \code{"BestEast_D"}).}

\item{north}{Name of the northing column in \code{collar}
(default \code{"BestNorth_D"}).}

\item{elev}{Name of the elevation column in \code{collar}
(default \code{"BestElev_D"}).}

\item{depth}{Name of the along-hole depth column in \code{survey}
(default \code{"Depth_M"}).}

\item{azimuth}{Name of the azimuth column in \code{survey} (degrees from north,
default \code{"AZIMUTH"}).}

\item{dip}{Name of the dip column in \code{survey} (degrees; negative =
downward, default \code{"DIP"}).}
}
\value{
A tibble with all original \code{survey} columns plus:
\describe{
\item{\code{from_x}, \code{from_y}, \code{from_z}}{Easting, northing, and elevation at
the start of the interval.}
\item{\code{to_x}, \code{to_y}, \code{to_z}}{Easting, northing, and elevation at the
end of the interval. Use with \code{\link[ggplot2:geom_segment]{ggplot2::geom_segment()}} via
\code{aes(x = from_x, xend = to_x, y = from_z, yend = to_z)}.}
\item{\code{mid_x}, \code{mid_y}, \code{mid_z}}{Easting, northing, and elevation at
the midpoint of the interval. Useful for joining assay data.}
}
The last station of each hole is dropped (it has no "to" depth).
}
\description{
Computes 3D positions along curved drillhole paths using the minimum
curvature method. Joins collar coordinates to survey measurements, then
accumulates position from the collar down through each survey interval.
Returns one row per survey interval with \code{mid_x}, \code{mid_y}, and \code{mid_z}
columns representing the easting, northing, and elevation at the midpoint
of that interval. The result can be piped directly into
\code{\link[=drillhole_section]{drillhole_section()}}.
}
\section{Method}{

For consecutive survey stations at depths \eqn{d_1} and \eqn{d_2} with
azimuth/dip \eqn{(\alpha_1, \delta_1)} and \eqn{(\alpha_2, \delta_2)},
the minimum curvature displacement is:
\deqn{\Delta x = \frac{L}{2}(l_1 + l_2) \cdot RF}
where \eqn{l_i = \cos(\delta_i)\sin(\alpha_i)}, \eqn{RF = \frac{2}{DL}
\tan\!\left(\frac{DL}{2}\right)}, and \eqn{DL} is the dog-leg angle
between the two stations. Dip follows the geological convention: negative
values are downward from horizontal (e.g. \eqn{-90} = straight down).
}

\examples{
collar <- data.frame(
  HOLEID      = c("DH-01", "DH-02"),
  BestEast_D  = c(1000, 1050),
  BestNorth_D = c(2000, 2010),
  BestElev_D  = c(500,  495)
)

survey <- data.frame(
  HOLEID  = rep(c("DH-01", "DH-02"), each = 4),
  Depth_M = rep(c(0, 50, 100, 150), 2),
  AZIMUTH = c(45, 46, 47, 48,  220, 221, 222, 223),
  DIP     = c(-60, -61, -62, -63,  -55, -56, -57, -58)
)

desurvey_holes(collar, survey)
}
